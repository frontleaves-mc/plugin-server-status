name: frontleaves-status
description: $description
prefix: FLSS
version: '${version}'

main: com.frontleaves.plugins.status.FrontleavesStatus
api-version: '1.21.1'
load: POSTWORLD

dependencies:
  server:
    frontleaves-lib:
      required: true
      load: BEFORE

authors: [ xiao_lfeng ]
website: https://www.frontleaves.com
